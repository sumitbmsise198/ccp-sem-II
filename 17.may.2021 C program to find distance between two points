#include <stdio.h>

    int main() {
    int a,b,a1,b1;
    double c ,d;
    printf("Enter the points:");
    scanf("%d%d",&a, &b, &a1, &b1);
    c= ((a-a1)*(a-a1))+((b-b1)*(b-b1));
    d = sqrt(c);
    printf("distance is %1f",d);
    return 0;
    }
